server:
  port: 8085

spring:
  profiles: r1
  application:
    name: inventory-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/,http://localhost:8083/eureka/

---

server:
  port: 8095

spring:
  profiles: r2
  application:
    name: inventory-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/,http://localhost:8083/eureka/


