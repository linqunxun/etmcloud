server:
  port: 8086

spring:
  application:
    name: etmcloudclient
  #cloud:
  #  config:
  #    uri: http://localhost:8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8082/eureka
feign:
  hystrix:
    enabled: true